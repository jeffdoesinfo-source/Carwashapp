import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carwash.staffmanagement',
  appName: 'Carwash Staff Management',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
